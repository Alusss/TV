name: Test

on:
  push:                # 代码推送时触发
  workflow_dispatch:   # 允许手动触发

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: test
        id: test
        run: |
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > release.jks
